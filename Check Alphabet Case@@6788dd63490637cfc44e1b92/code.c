#include <stdio.h>
int main(){
    char ch;
    scanf("%c",&ch);
    if(uppercase(ch)){
        printf("Lowercase");
    }
    else if{
        printf("Lowercase");
    }
    else{
        printf("Not an alphabet");
    }
    return 0;
}
