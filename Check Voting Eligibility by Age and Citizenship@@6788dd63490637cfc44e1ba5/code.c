#include <stdio.h>
int main(){
    int age,citizen status;
    scanf("%d %d",&a,&b);
    if(age>=18){
        printf("Eligible");
    }
    else{
        printf("Not Eligible");
    }
    return 0;
}